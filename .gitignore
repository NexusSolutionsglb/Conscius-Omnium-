# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# misc
.DS_Store
*.pem
Thumbs.db

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files
.env
.env*.local
.env.production

# vercel
.vercel

# cloudflare (opennextjs-cloudflare / wrangler)
.open-next
.wrangler
.dev.vars
.dev.vars.*
!.dev.vars.example
cloudflare-env.d.ts

# typescript
*.tsbuildinfo

# supabase
/supabase/.branches
/supabase/.temp
/supabase/.env

# editor / os
/legacy/**/node_modules
.idea/
.vscode/*
!.vscode/extensions.json

# the raw camera master (37 MB) — keep it locally, never commit it.
# The web-encoded clip + poster in /public/hero-media ARE committed (they
# ship with the Cloudflare deploy as static assets — ~12 MB / ~8.5 MB, both
# under the 25 MiB per-file limit).
/hero video.mp4
