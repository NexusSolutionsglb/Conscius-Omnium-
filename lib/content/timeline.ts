import type { TimelineEntry } from "@/lib/types";

/**
 * No biographical timeline was supplied in the client's material — left
 * empty rather than invented. The Admin adds entries here as the real
 * record becomes available.
 */
export const timelineSeed: TimelineEntry[] = [];
