import type { Exhibition } from "@/lib/types";

/**
 * No exhibition history was supplied in the client's material — left empty
 * rather than invented. The Admin adds entries here as the real record
 * becomes available.
 */
export const exhibitionsSeed: Exhibition[] = [];
